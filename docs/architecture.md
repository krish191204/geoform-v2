# Architecture

Geoform v1 is a 4-stage pipeline (Sketch / Critique / Make sense / Worldbuild) running entirely in TypeScript on the client. Python/WorldEngine is an optional CI-time validator only.

This document is a Phase 0 placeholder. Phase 3 will expand it.